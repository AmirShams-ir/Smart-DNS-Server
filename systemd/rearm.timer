[Unit]
Description=Automatic Smart DNS Rearm

[Timer]
# The interval is managed by /etc/systemd/system/rearm.timer.d/interval.conf
# and synchronized with config/defaults.conf by the Rearm Manager.
OnUnitActiveSec=1h
AccuracySec=1min
Unit=rearm.service

[Install]
WantedBy=timers.target
